<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="TabItem" TargetType="{x:Type Button}">
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Height" Value="29"/>
        <Setter Property="Margin" Value="2 2 2 0"/>
        <Setter Property="Foreground" Value="#333"/>
        <Setter Property="FontFamily" Value="Tahoma"/>
        <Setter Property="FontSize" Value="12"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid x:Name="grid" >
                        <!--<Border x:Name="border1" CornerRadius="10,10,10,10" 
                     BorderBrush="red" BorderThickness="0" Padding="10" Margin="0,7,0,0" Background="White" >
                            <Border.Effect>
                                <DropShadowEffect Direction="90" ShadowDepth="1" BlurRadius="3" Color="black" Opacity="0.2" ></DropShadowEffect>
                            </Border.Effect>
                        </Border>-->

                        <Border x:Name="border" CornerRadius="6 6 0 0" BorderBrush="#2666"
                                BorderThickness="1 1 1 0" Background="WhiteSmoke"  Padding="5 0 0 0">

                            <ContentPresenter 
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                TextElement.FontWeight="Normal">
                            </ContentPresenter>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" TargetName="border">
                                <Setter.Value>
                                    <RadialGradientBrush GradientOrigin="0.496,1.052">
                                        <RadialGradientBrush.RelativeTransform>
                                            <TransformGroup>
                                                <ScaleTransform CenterX="0.5" 
                                            CenterY="0.5" ScaleX="1.5" ScaleY="1.5"/>
                                                <TranslateTransform X="0.02" Y="0.3"/>
                                            </TransformGroup>
                                        </RadialGradientBrush.RelativeTransform>
                                        <GradientStop Color="#40b5f2" Offset="0.3"/>
                                        <GradientStop Color="#40b5f2" Offset="0.3"/>
                                    </RadialGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="border"  Value="white"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" TargetName="grid" Value="0.25"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TabItemSelected" TargetType="{x:Type Button}" BasedOn="{StaticResource TabItem}" >
        <Setter Property="Foreground" Value="#222"/>
        <Setter Property="Template" >
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid x:Name="grid" >
                        <!--<Border x:Name="border1" CornerRadius="10,10,10,10" 
                     BorderBrush="red" BorderThickness="0" Padding="10" Margin="0,7,0,0" Background="White" >
                            <Border.Effect>
                                <DropShadowEffect Direction="90" ShadowDepth="1" BlurRadius="3" Color="black" Opacity="0.2" ></DropShadowEffect>
                            </Border.Effect>
                        </Border>-->

                        <Border x:Name="border" CornerRadius="6 6 0 0" BorderBrush="#2666" 
                                BorderThickness="1 1 1 0" Background="White"  Padding="5 0 0 0">
                            <ContentPresenter 
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Center"
                                      TextElement.FontWeight="Normal">
                            </ContentPresenter>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" TargetName="grid" Value="0.25"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TabItemCloseBTN" TargetType="{x:Type Button}">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Width" Value="28"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid Background="Transparent">
                        <Path x:Name="CloseIcon" Width="10" Height="10"
                              Data="M 0,0 10,10 M 0,10 10,0"
                              Stroke="#222" StrokeThickness="1"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="CloseIcon" Property="Stroke" Value="Black"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
